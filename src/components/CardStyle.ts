import { makeStyles } from '@mui/material/styles';

const useStyles = makeStyles({
  root: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  icon: {
    position: 'absolute',
    top: '8px',
    right: '8px',
  },
});
